function make_log(profile_id,entity,operation,subject,full_name){
	/*frappe.call({
		method:'phr.phr.doctype.phr_activity_log.make_log',
		args:{'profile_id':profile_id,"entity":entity,"operation":operation,"subject",:subject,"full_name":full_name}
	})*/
}